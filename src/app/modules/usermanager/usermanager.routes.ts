import { Routes } from '@angular/router';
import { UserManagerComponent } from './usermanager.component';

export const USERMANAGER_ROUTES: Routes = [
  {
    path: '',
    component: UserManagerComponent
  }
];
